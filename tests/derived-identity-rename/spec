# The derived move-and-copy document (a delete block plus two creations, no
# rename or copy headers) travels the normal intake as both operands and the
# identity run stays silent with a zero exit.
bin: DIFFOFDIFFS
args: p1.patch p2.patch
exit: 0
