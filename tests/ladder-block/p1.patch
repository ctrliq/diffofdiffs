--- a/probe.c
+++ b/probe.c
@@ -7,6 +7,9 @@
 	err = hw_init(d);
 	if (err < 0)
 		return err;
+	trace_enter(d);
+	d->busy = 1;
+	d->ready = 1;
 	dev_ready(d);
 	d->flags |= LIVE;
 	return err;
@@ -13,6 +16,6 @@
 }
 static void probe_arm(struct dev *d)
 {
-	d->epoch = 0;
+	d->epoch = 1;
 	spin_lock(&d->slock);
 	d->count++;
