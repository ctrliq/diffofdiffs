--- a/drivers/net/thing/main.c
+++ b/drivers/net/thing/main.c
@@ -32,6 +32,8 @@ static int thing_probe(struct thing *dev)
 	int ret;
 
 	ret = setup_ring(dev);
+	flush_queue(dev);
+	drain_cache(dev);
 	arm_timer(dev);
 	return 0;
 }
