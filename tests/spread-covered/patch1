--- a/drivers/net/probe.c
+++ b/drivers/net/probe.c
@@ -1,6 +1,6 @@
 void probe(struct dev *d)
 {
-	spread_old_one(d);
+	spread_new_one(d);
 	sync_all(d);
 	wake_up(d);
 }
@@ -40,4 +40,4 @@ second
 ctxq
-	spread_old_two(d);
-	filler_add(d);
+	spread_new_two(d);
+	filler_del(d);
 ctxr
