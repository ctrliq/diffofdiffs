# The agreed line is a lone tab rather than an empty line: whitespace-only is
# the class the sweep covers, byte identity still binds, and the fully covered
# hunk is filtered out, so the run prints nothing.
bin: DIFFOFDIFFS
args: patch1 patch2
exit: 0
