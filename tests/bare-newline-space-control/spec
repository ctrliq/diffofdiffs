# The patches of bare-newline-delta with the blank context line spelled with its
# leading space. The spelling must make no difference, so the expected output
# matches bare-newline-delta's byte for byte.
bin: DIFFOFDIFFS
args: patch1 patch2
exit: 0
