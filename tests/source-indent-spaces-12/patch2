--- a/f
+++ b/f
@@ -28,7 +28,8 @@
     	if (ready) {
     		struct item *item = get_item();
 
-    		if (condition_a() &&
+    		if (type_ok() &&
+    		    condition_a() &&
     		    item_ready(item)) {
     			work(item);
     		}
