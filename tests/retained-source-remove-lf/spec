# retained-source-add-lf with the final newline the other way round: patch1's D
# and X end with a newline, and patch2's D doesn't. The report must put the
# no-newline marker after patch2's D only.
bin: DIFFOFDIFFS
args: patch1 patch2
exit: 0
