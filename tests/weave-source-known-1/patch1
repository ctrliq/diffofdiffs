--- a/drivers/net/probe.c
+++ b/drivers/net/probe.c
@@ -3,2 +3,11 @@
-	old_reset(d);
-	old_start(d);
+	init_ring(d);
+	fill_ring(d);
+	arm_ring(d);
+	set_flags(d, NEW_FLAG);
+	map_bars(d);
+	claim_irq(d);
+	start_queue(d);
+	report(d, "attach");
+	enable_tx(d);
+	enable_rx(d);
+	kick(d);
@@ -6 +15 @@
-	wake_up(d);
+	wake_more(d);
