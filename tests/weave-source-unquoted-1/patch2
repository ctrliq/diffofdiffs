--- a/drivers/net/probe.c
+++ b/drivers/net/probe.c
@@ -2,4 +2,13 @@
 {
-	legacy_reset(d);
-	legacy_start(d);
+	init_ring(d);
+	fill_ring(d);
+	arm_ring(d);
+	set_flags(d, OLD_FLAG);
+	map_bars(d);
+	claim_irq(d);
+	start_queue(d);
+	report(d, "probe");
+	enable_tx(d);
+	enable_rx(d);
+	kick(d);
 	sync_all(d);
