# The retained source identifies the nested check insertion across indentation
# changes. Keep its literal difference and the separate additions inside the
# earlier shared result.
bin: DIFFOFDIFFS
args: p1.patch p2.patch
exit: 0
