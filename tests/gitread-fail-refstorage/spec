# extensions.refstorage naming reftable at format version 0: the library
# only enforces extensions at version 1, so the store opens fine and the
# reader's own guard is what refuses, naming the backend. The version-1
# spelling is refused by the library itself with the generic open refusal.
bin: GITREAD_UNIT
args: fail-refstorage
exit: 1
