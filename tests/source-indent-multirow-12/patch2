--- a/f
+++ b/f
@@ -28,8 +28,9 @@
 		if (ready) {
 			struct item *item = get_item();
 
-			if (condition_a() &&
-			    item_ready(item)) {
+			if (type_ok() &&
+			    condition_a() &&
+			    item_valid(item)) {
 				work(item);
 			}
 		}
