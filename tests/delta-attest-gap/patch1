--- a/drivers/att/main.c
+++ b/drivers/att/main.c
@@ -7,3 +7,3 @@ static void att_prepare(struct att *a)
 	att_flush(a);
-	old_call(a);
+	new_call(a);
 	att_sync(a);
@@ -11,3 +11,3 @@ static void att_prepare(struct att *a)
 	att_poll(a);
-	att_arm(a);
+	att_arm_fast(a);
 	att_run(a);
