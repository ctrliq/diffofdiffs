# Patch1 removes line 4 (delta) and patch2 removes lines 4 and 5 (delta and
# echo), both with no context lines. Delta's removal must show as an edit both
# patches make and echo's removal as patch2's alone. attest-f7 is the same
# change with context.
bin: DIFFOFDIFFS
args: patch1 patch2
exit: 0
