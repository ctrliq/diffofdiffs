# The cifs lock conversion pair of consumed-fa-tie, with one extra patch1 hunk
# in cifs_pick_channel() that patch2 lacks. The shared conversions must be left
# out; patch1's cifs_pick_channel() hunk must show on its side and patch2's
# compound_send_recv() hunk on the other.
bin: DIFFOFDIFFS
args: patch1 patch2
exit: 0
