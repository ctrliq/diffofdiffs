--- a/drivers/thing.c
+++ b/drivers/thing.c
@@ -9,19 +9,9 @@
 static void keep_me(void)
 {
 	tick();
 }
 
-static void doomed(int n)
-{
-	struct sk_buff *skb;
-
-	if (n > 0)
-		read_stamp(skb);
-
-	complete(skb);
-}
-
 static void after(void)
 {
 	done();
 }
