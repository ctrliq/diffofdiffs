--- a/mod.c
+++ b/mod.c
@@ -6,3 +6,4 @@
 	start(dev);
+	arm(dev);
 	claim(dev);
 	ready(dev);
@@ -10,7 +11,12 @@
 	lead_one();
 	lead_two();
 	charlie();
+	helper_one();
+	helper_two();
+	helper_five();
+	helper_six();
 	tail_one();
 	if (dev->on) {
+helper_four();
 		run(dev);
 	}
@@ -40,4 +46,4 @@
 	lead_one();
 	lead_two();
-	omega_old();
+	omega_new();
 	tail_one();
