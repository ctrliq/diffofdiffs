--- a/g.c
+++ b/g.c
@@ -1,3 +1,3 @@
 g_one
 g_two
-mode = OLD;
+mode = SECOND;
--- a/f.c
+++ b/f.c
@@ -110,5 +110,5 @@
 p2only_a
 p2only_b
-	guard(old_limit);
+	guard(new_limit);
 inner_shared
 tail_shared
\ No newline at end of file
