--- a/f
+++ b/f
@@ -21,8 +21,9 @@
 	if (ready) {
 		struct item *item = get_item();
 
-		if (condition_a() &&
-		    item_ready(item)) {
+		if (type_ok() &&
+		    condition_a() &&
+		    item_present(item)) {
 			work(item);
 		}
 	}
