--- a/f
+++ b/f
@@ -21,7 +21,8 @@
 	if (ready) {
 		struct item *item = get_item();
 
-		if (condition_a() &&
+		if (type_ok() &&
+		    condition_a() &&
 		    item_ready(item)) {
 			work(item);
 		}
