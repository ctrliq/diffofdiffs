--- a/drivers/thing.c
+++ b/drivers/thing.c
@@ -11,18 +11,7 @@
 
 	prepare(t);
 
-	if (t->enc) {
-		rc = copy_sgl(t->src, t->dst);
-		if (rc)
-			goto free;
-		rc = encrypt(t);
-	} else {
-		rc = copy_sgl(t->dst, t->src);
-		if (rc)
-			goto free;
-		rc = decrypt(t);
-	}
-	sync_state(t);
+	handle_all(t);
 
 free:
 	release(t);
