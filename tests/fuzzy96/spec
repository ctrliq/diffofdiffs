# The upstream patch does not establish that the two blanks removed by the
# backport survive at that source location. Do not restore those blanks from
# failed-application state.
bin: DIFFOFDIFFS
args: patch1 patch2
exit: 0
